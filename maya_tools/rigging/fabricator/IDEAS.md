- RESERVED NAME: "Library" is held for a future asset-library tool (fast asset finding/browsing). The pose/anim tools are named Pose Studio / Animation Studio partly to keep this name free (Adrian, 2026-07-11).
- Integrate Marionette mocap into the rig/animation pipeline.
- ANNOTATE: the reverse-node math gives foot_roll_pivot.rotateZ = 1° at rest (not 0°), since 1 - 0 = 1 — deliberate legacy-parity choice for solidness. A future TA would benefit from the math being annotated where it lives (IKLeg reverse foot).
- HARDEN: Save Template should require Edit Mode (MODE_EMPTY / MODE_SKELETON). When the rig is built (any component `is_built=True`), `_snapshot_skeleton_from_scene` falls back to `bind_pose_json` for local TRS but the corresponding `world_translate` is set to `list(translate)` — correct for roots, wrong for child joints (their local translate is offset-from-parent, not world position). Reload into a fresh scene produces a broken skeleton hierarchy. Fix: gate `fs_app.save` (or the File → Save Template handler) with a built-rig check, throw a clear `RuntimeError`/log line telling the user to unbuild first. Cheaper than computing forward-kinematics world positions from the bind chain. Adrian validated this manually 2026-05-10 — Edit-mode save+load round-trips cleanly; Rig-mode save produces a broken reload.
- REFACTOR: extract `nodes.find_component_node_by_id(instance.id)` helper-call to replace the duplicated `next((cn for cn in nodes.get_all_component_nodes() if nodes.get_component_id(cn) == instance.id), '')` pattern. Already 4+ call sites across `modules/world.py`, `modules/simple_fk.py`, `modules/advanced_fk.py`, and the static-Contract loop in `fs_app.build_modules`. The helper exists at `nodes.py:180`; Task 3 of the AdvancedFK sprint used it for the new code but left the pre-existing duplications alone for scope hygiene. One-pass module-cleanup commit.
- CONSIDER: cycle detection for AdvancedFK space targets. If two AdvancedFK ctrls reference each other's joint as a space, the DG forms a cycle — Maya surfaces a cycle warning at evaluation. Authoring-time UI validation (`_build_spaces_section`) catches typos but not cycles. Worth a build-time pass that walks the resolved space-graph and raises a clear `RuntimeError` if a cycle is detected. Defer until an artist hits it; the warning is informative as-is.
- DESIGN: Section authors registered by `contract.type`. `properties_panel.py` is large, with `_build_component_section` handling everything inline + per-type gates (AdvancedFK Spaces section; more components will want their own). A registry of `section_builders: dict[str, callable]` keyed on `contract.type` would let each component opt into one or more custom sections without growing the central method. Defer until a third component needs a custom section.
